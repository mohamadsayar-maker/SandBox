const short={index:'I',middle:'M',ring:'R',pinky:'P'};
function summary(hand){const d=hand.gestures.thumbDistances;return`${hand.id.slice(0,1)} ${Object.keys(short).map(k=>`${short[k]} ${Math.round(d[k].px)}`).join(' · ')}`}
function attach(){if(!globalThis.HandKit)return setTimeout(attach,50);HandKit.subscribe(frame=>{const mode=globalThis.__HANDKIT_MODE||'lab',lab=mode==='lab',numbers=mode==='numbers',gaps=frame.hands.map(summary),hud=document.getElementById('gestureHud'),gap=document.getElementById('gapPill'),gapM=document.getElementById('gapM'),count=document.getElementById('countPill'),countM=document.getElementById('countM'),label=document.getElementById('gestureName');if(hud)hud.style.display=lab?'flex':'none';if(gap&&lab)gap.textContent=gaps.length?`Thumb gaps ${gaps.join('  |  ')} px`:'Thumb gaps –';if(gapM)gapM.textContent=gaps.join('  |  ')||'–';if(count)count.style.display='none';if(countM?.parentElement)countM.parentElement.style.display=numbers?'':'none';if(!lab&&label){label.classList.remove('show');label.textContent=''}})}
attach();
